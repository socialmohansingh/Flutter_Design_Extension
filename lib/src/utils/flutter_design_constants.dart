class FlutterDesignConstants {
  /// [packageName] !!! should always match !!!
  /// the one specified in pubspec.yaml.
  ///
  /// Usage: use inside TextStyle definitions, so that other packages can access
  /// the font-assets (.ttf) from this package without the need to declare them.
  static const String packageName = "flutter_design_extension";
}
