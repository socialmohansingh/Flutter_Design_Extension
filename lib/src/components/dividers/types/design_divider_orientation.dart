/// Enum denoting orientation of the divider, [horizontal] or [vertical].
enum DesignDividerOrientation {
  /// Makes the divider orientation as horizontal
  horizontal,

  /// Makes the divider orientation as vertical
  vertical,
}
